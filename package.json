{
  "name": "shooter-game",
  "version": "1.0.0",
  "description": "Top Down Shooter Game made using Phaser 3",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open --no-hmr --host 0.0.0.0 --port 3000",
    "build": "parcel build index.html"
  },
  "dependencies": {
    "phaser": "3.15.1"
  },
  "devDependencies": {
    "@babel/core": "7.2.0",
    "parcel-bundler": "^1.6.1"
  }
}
