{
  "name": "shooter-game",
  "version": "1.0.0",
  "description": "Top Down Shooter Game made using Phaser 3",
  "main": "index.html",
  "scripts": {
    "start": "parcel watch ./index.html --no-hmr & live-server --port=3000 --host=0.0.0.0 ./dist/",
    "build": "parcel build index.html"
  },
  "dependencies": {
    "phaser": "^3.16.2"
  },
  "devDependencies": {
    "@babel/core": "7.3.4",
    "live-server": "^1.2.1",
    "parcel-bundler": "^1.6.1"
  }
}
